import React from 'react';
import {View} from 'react-native';
import Typography from '../../components/Typography';

const Home = () => {
  return (
    <View>
      <Typography size={24} weight={700} color={'main'}>
        Home
      </Typography>
    </View>
  );
};

export default Home;
